import ActionButton from "~components/shared/ActionButton";
import * as React from "react";

function ActionButtons() { //tslint:disable-line
    return (
        <div>
          <ActionButton />;
    </div>
    );
}

export { ActionButtons };
